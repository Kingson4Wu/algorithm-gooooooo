package _02510

func findUnsortedSubarray(nums []int) int {

	/*lastIndex := 0
	maxVal := nums[0]
	change := false
	for i := 0; i < len(nums); i++ {
		if nums[i] < nums[i-1] {
			change = true
		}
		if nums[i] > maxVal {
			if change {
				lastIndex = i
			}
			maxVal = nums[i]
		}
	}*/

	return 0
}
